# PRD: 딥씨 다이브 (가칭) — 레이디 버그 오마주 캐주얼 게임

## 1. 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 딥씨 다이브 (가칭, 추후 확정 필요) |
| 한 줄 설명 | 바다 속을 헤엄치며 쏟아지는 해파리를 피하고, 아이템으로 화면을 정리하며 최대한 오래 생존하는 캐주얼 서바이벌 게임 |
| 레퍼런스 원작 | 레이디 버그 (2010~2011, 개발 donxu, 원스토어 출시) |
| 배포 형태 | Project Hub 내 웹 게임 (모바일 웹 + 데스크톱 브라우저 지원) |

---

## 2. 배경 및 목표

이번 프로젝트는 기존 정보 조회형 프로젝트(맛집 찾기, 미세먼지, 날씨 등)와 달리 **실시간 렌더링 루프가 있는 게임**을 처음 만들어보는 것이 핵심 학습 목표다. Canvas 기반 게임 루프, 충돌 판정, 입력 처리, 그리고 처음으로 **읽기/쓰기가 모두 필요한 백엔드(Supabase)**를 다루는 경험을 쌓는다.

원작 "레이디 버그"는 단순한 규칙(위에서 쏟아지는 적 피하기 + 아이템으로 화면 정리) 안에 난이도 곡선과 아이템 시너지가 잘 짜여 있어서, 클론이 아닌 **오마주**로 핵심 재미 요소만 가져오고 소재(무당벌레/꽃 → 심해 생물)와 조작 방식(기울이기 → 드래그)을 웹 환경에 맞게 바꾼다.

---

## 3. 원작 레퍼런스 분석 (레이디 버그)

원작에서 가져올 핵심 재미 요소:

- **조작 → 생존의 단순한 루프**: 기울여서 피하기만 해도 게임이 성립하지만, 아이템을 어떻게 쓰느냐로 실력 차이가 벌어짐
- **아이템 = 일시적 안전지대 생성**: 대부분의 아이템이 "잠깐의 무적/차단 구간"을 만들어주는 방식이라 압박감과 해방감이 반복됨
- **점수 구간별 난이도 증가**: 특정 점수를 넘으면 적이 등장하는 방향이 늘어나며 후반으로 갈수록 조여옴
- **레벨업 → 아이템 해금**: 처음엔 기본 아이템 하나뿐이지만 레벨이 오르며 새 아이템이 순차적으로 풀림

이 구조를 그대로 유지하되, **아이템 개별 강화(L코인 시스템)는 제외**하고 해금 순서 중심의 단순한 레벨업으로 축소한다 (표준 재현 범위 결정에 따름).

---

## 4. 오마주 컨셉

| 원작 | 오마주 |
|---|---|
| 무당벌레 | 작은 물고기 (플레이어 캐릭터) |
| 정원 배경 | 바다 속 배경 |
| 빨간 개미(적) | 해파리 (위에서 쏟아지며 접촉 시 즉시 게임오버) |
| 꽃/하트 아이템 | 산호/기포/조개 등 바다 테마 아이템 |

> **가정**: "잠수함/물고기" 중 플레이어 캐릭터는 **작은 물고기**로 진행합니다. 잠수함은 필요 시 후속 테마 확장(다른 스킨)으로 남겨두는 걸 제안드려요. 이견 있으시면 알려주세요.

---

## 5. 핵심 게임 루프

1. 화면 상단에서 해파리가 지속적으로 쏟아져 내려옴
2. 플레이어는 화면을 클릭/탭해 물고기를 원하는 위치로 이동시켜 해파리를 피함
3. 화면에 랜덤 등장하는 아이템을 먹으면 즉시 스킬 발동 (화면의 해파리 일부/전체 제거, 또는 일시 무적)
4. 생존 시간에 비례해 점수가 오르고, 해파리를 제거하면 추가 점수 획득
5. 점수 구간을 넘을 때마다 레벨업 → 새 아이템 해금 + 해파리 스폰 난이도 상승
6. 해파리에 닿으면 즉시 게임오버 → 닉네임 입력 → 랭킹 등록

---

## 6. 기능 명세

### 6.1 조작
- **방식**: 홀드 투 무브. 화면을 꾹 누르고 있으면 물고기가 포인터 방향으로 가속하며 헤엄침. 누른 채로 포인터를 움직이면 그 방향을 계속 따라감
- 손을 떼면 즉시 멈추지 않고 **관성**으로 잠시 미끄러지다가 감속해 정지 (원작의 "완급 조절"을 물리 기반 가감속으로 재현)
- 가속·감속·최고 속도·관성 계수는 `src/game/constants.ts` (`FISH_ACCEL`, `FISH_DRAG`, `FISH_MAX_SPEED`) 로 조정
- 벽에 닿으면 해당 축 속도를 죽여 미끄러짐 방지

### 6.2 캐릭터 & 충돌
- 물고기와 해파리의 충돌 판정은 원형(반지름 기반) 충돌로 단순화
- 해파리와 충돌 시 즉시 게임오버 (원작과 동일, 목숨 개념 없음)

### 6.3 적(해파리) 스폰 & 난이도
- 기본: 화면 상단에서만 랜덤 위치로 스폰, 아래로 낙하
- 점수 구간 도달 시 스폰 방향 추가 (예: 1만 점 이상 → 좌측에서도 등장, 3만 점 이상 → 우측에서도 등장) — 원작의 "10만/30만점 방향 추가" 구조를 축소된 점수 스케일에 맞게 재조정
- 시간 경과에 따라 낙하 속도 및 스폰 빈도 점진적 증가

### 6.4 아이템 시스템 (5종)

원작 10종 중 반응이 좋았던 핵심 아이템만 선별해 5종으로 재구성했습니다.

| 아이템 (오마주) | 원작 대응 | 해금 레벨 | 효과 |
|---|---|---|---|
| 거품 실드 | 꽃잎 폭탄 | 1 (기본 지급) | 즉시 발동, 일정 시간(3초) 무적. 종료 직전에는 방어막 링이 점멸하며(남은 시간이 짧을수록 빠르게) 곧 풀림을 알림 |
| 산호가시 유도탄 | 나뭇잎 유도탄 | 2 | 픽업 후 지속 시간(5초) 동안 1초마다 소량(2발)씩 연사. 각 가시는 랜덤 해파리를 추적, 명중 시 제거 |
| 가시복 모드 | 꽃잎 보호막 | 3 | 일정 시간(5초) 무적 + 접촉한 해파리 자동 제거 (실드와 동일하게 종료 직전 점멸) |
| 고래상어 라이드 | 벌 미사일 | 4 | 느리게 전진하는 거대 고래상어 소환, 닿는 해파리 모두 제거 (안전지대 역할) |
| 산호 배리어 | 코스모스 | 5 | 물고기 주변에 보호막 여러 개 생성 → 일정 시간 후 전방 발사되며 화면 정리 |

- 아이템은 화면에 최대 2~3개까지 동시 등장, 랜덤 스폰
- 개별 강화(레벨/지속시간 업그레이드)는 MVP에서 제외 — 해금되면 고정 스펙으로 동작
- 밸런스 수치는 `src/game/items.ts` 로 조정 (`SHIELD_BLINK_START_MS`, `MISSILE_COUNT`, `MISSILE_VOLLEY_INTERVAL_MS`, `MISSILE_BARRAGE_DURATION_MS` 등)
- **개발용 토글**: `src/game/constants.ts` 의 `TEST_SPAWN_ALL_ITEMS` — true 면 시작 즉시 5종 아이템이 하나씩 배치되고 레벨 해금과 무관하게 전부 스폰됨. 배포 전 false 로 되돌릴 것

### 6.5 레벨업 시스템
- 원작의 "레벨 55 + 아이템별 세부 강화" 대신, **점수 구간 기반 5단계 레벨업**으로 단순화
- 레벨업 시 새 아이템 1종 해금 + 해파리 스폰 난이도 소폭 상승
- 레벨 5 도달 이후엔 아이템 해금 없이 난이도만 계속 상승 (엔드리스 서바이벌)

### 6.6 스코어링
- 기본 점수: 생존 시간에 비례해 초당 증가
- 보너스 점수: 아이템 효과로 해파리 제거 시 마리당 추가 점수
- 최종 점수 = 생존 점수 + 제거 보너스 합산

### 6.7 랭킹 시스템
- **DB**: Supabase (Postgres)
- **범위**: 전체 기간 TOP N (예: TOP 20, 정확한 N은 구현 단계에서 확정)
- **등록 방식**: 게임오버 후 닉네임 입력 (별도 인증 없음, 익명)
- **리스크(사전 공유)**: 인증이 없는 익명 랭킹이라 클라이언트 조작으로 비정상 점수가 등록될 가능성이 있습니다. MVP에서는 서버 측에 "생존 시간 대비 점수 상한" 같은 최소한의 개연성 검증만 넣고, 정교한 부정행위 방지(리플레이 검증 등)는 범위에서 제외하는 걸 제안드려요. 필요하시면 이후 버전에서 강화 가능합니다.

### 6.8 화면 구성
1. **시작 화면**: 타이틀, 시작 버튼, 랭킹 보기 버튼, 조작 방법 간단 안내
2. **게임 화면**: 캔버스 게임 영역 + 상단 HUD(점수, 레벨)
3. **게임오버 화면**: 최종 점수, 닉네임 입력 폼, 등록 버튼
4. **랭킹 화면**: TOP N 리스트 (순위/닉네임/점수)

---

## 7. 기술 스택

| 영역 | 기술 |
|---|---|
| 프레임워크 | React 19 + Vite + TypeScript |
| 렌더링 | Canvas API (게임 루프 requestAnimationFrame 기반) |
| 스타일 | CSS Modules (Project Hub 디자인 시스템 상속: 오프화이트 배경, 테라코타 포인트 컬러 등 — 단, 게임 내부는 심해 테마 색상 별도 적용) |
| 백엔드/DB | Supabase (Postgres, 랭킹 저장/조회) |
| 배포 | Vercel |

---

## 8. 데이터 모델 (Supabase)

```sql
create table scores (
  id uuid primary key default gen_random_uuid(),
  nickname varchar(20) not null,
  score integer not null,
  survived_seconds integer not null,
  created_at timestamptz not null default now()
);

-- 최소한의 개연성 검증 (완전한 부정행위 방지 아님, MVP 수준)
alter table scores add constraint plausible_score
  check (score <= survived_seconds * 500); -- 초당 최대 점수 상한 예시, 실제 밸런싱 후 조정
```

- 읽기: 누구나 TOP N 조회 가능 (RLS: public select)
- 쓰기: 누구나 등록 가능하나 위 constraint로 최소 검증 (RLS: public insert with check)

---

## 9. Non-goals (MVP 제외 범위)

- 로그인/회원가입, 인증 기반 랭킹
- 아이템 개별 강화(L코인 같은 재화 시스템)
- 일간/주간 랭킹, 랭킹 리셋
- 정교한 부정행위 방지(리플레이 검증, 서버 사이드 시뮬레이션 등)
- 모바일 네이티브 앱, 자이로센서 조작
- 소셜 공유, 친구 랭킹
- 사운드/BGM (필요 시 별도 논의)

---

## 10. 열린 질문

- [ ] 게임 제목 최종 확정 (가칭 "딥씨 다이브")
- [ ] 플레이어 캐릭터를 물고기로 확정할지, 잠수함 옵션도 고려할지
- [ ] 랭킹 TOP N 숫자 (기본 제안: 20)
- [ ] 초당 점수 상한 등 밸런싱 수치는 구현 중 플레이테스트로 조정 필요
- [ ] 닉네임 욕설/부적절 필터링 여부 (MVP에서는 미포함 제안, 필요시 간단한 금칙어 필터 추가 가능)

---

## 11. 마일스톤 (제안)

1. **핵심 루프 프로토타입**: 캐릭터 이동 + 해파리 스폰/충돌 + 게임오버 (아이템/랭킹 없이)
2. **아이템 시스템**: 5종 아이템 스폰/효과 구현
3. **레벨업 & 난이도 곡선**: 점수 구간별 해금/난이도 상승
4. **Supabase 연동**: 랭킹 등록/조회, 닉네임 입력 UI
5. **폴리싱 & 배포**: UI/UX 다듬기, Project Hub 통합, Vercel 배포
